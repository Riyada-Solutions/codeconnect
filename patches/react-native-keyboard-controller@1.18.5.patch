diff --git a/src/components/KeyboardAwareScrollView/index.tsx b/src/components/KeyboardAwareScrollView/index.tsx
index 4e2b22c0b47f40dbcd1801dc2d301ec6c8aac942..fcd7fdfadbb0fcf8d9996bc4af17f83acaa30534 100644
--- a/src/components/KeyboardAwareScrollView/index.tsx
+++ b/src/components/KeyboardAwareScrollView/index.tsx
@@ -155,21 +155,26 @@ const KeyboardAwareScrollView = forwardRef<
      * Function that will scroll a ScrollView as keyboard gets moving.
      */
     const maybeScroll = useCallback(
-      (e: number, animated: boolean = false) => {
+      (e: number, animated?: boolean) => {
         "worklet";
 
         if (!enabled) {
           return 0;
         }
 
-        // input belongs to ScrollView
-        if (layout.value?.parentScrollViewTarget !== scrollViewTarget.value) {
+        // input belongs to ScrollView (avoid ?. in worklets — Hermes parse issues)
+        const layoutNow = layout.value;
+        if (
+          layoutNow == null ||
+          layoutNow.parentScrollViewTarget !== scrollViewTarget.value
+        ) {
           return 0;
         }
 
         const visibleRect = height - keyboardHeight.value;
-        const absoluteY = layout.value?.layout.absoluteY || 0;
-        const inputHeight = layout.value?.layout.height || 0;
+        const innerLayout = layoutNow.layout;
+        const absoluteY = innerLayout != null ? innerLayout.absoluteY : 0;
+        const inputHeight = innerLayout != null ? innerLayout.height : 0;
         const point = absoluteY + inputHeight;
 
         if (visibleRect - point <= bottomOffset) {
@@ -189,7 +194,12 @@ const KeyboardAwareScrollView = forwardRef<
           const targetScrollY =
             Math.max(interpolatedScrollTo, 0) + scrollPosition.value;
 
-          scrollTo(scrollViewAnimatedRef, 0, targetScrollY, animated);
+          scrollTo(
+            scrollViewAnimatedRef,
+            0,
+            targetScrollY,
+            animated === true,
+          );
 
           return interpolatedScrollTo;
         }
@@ -202,7 +212,7 @@ const KeyboardAwareScrollView = forwardRef<
             scrollViewAnimatedRef,
             0,
             topOfScreen - positionOnScreen,
-            animated,
+            animated === true,
           );
         }
 
@@ -228,19 +238,22 @@ const KeyboardAwareScrollView = forwardRef<
     const updateLayoutFromSelection = useCallback(() => {
       "worklet";
 
-      const customHeight = lastSelection.value?.selection.end.y;
+      const lastSel = lastSelection.value;
+      const customHeight =
+        lastSel != null ? lastSel.selection.end.y : undefined;
 
-      if (!input.value?.layout || !customHeight) {
+      const inputVal = input.value;
+      if (inputVal == null || !inputVal.layout || !customHeight) {
         return false;
       }
 
       layout.value = {
-        ...input.value,
+        ...inputVal,
         layout: {
-          ...input.value.layout,
+          ...inputVal.layout,
           // when we have multiline input with limited amount of lines, then custom height can be very big
           // so we clamp it to max input height
-          height: clamp(customHeight, 0, input.value.layout.height),
+          height: clamp(customHeight, 0, inputVal.layout.height),
         },
       };
 
@@ -274,8 +287,9 @@ const KeyboardAwareScrollView = forwardRef<
       (e: FocusedInputSelectionChangedEvent) => {
         "worklet";
 
-        const lastTarget = lastSelection.value?.target;
-        const latestSelection = lastSelection.value?.selection;
+        const prevSel = lastSelection.value;
+        const lastTarget = prevSel != null ? prevSel.target : undefined;
+        const latestSelection = prevSel != null ? prevSel.selection : undefined;
 
         lastSelection.value = e;
 
@@ -285,9 +299,11 @@ const KeyboardAwareScrollView = forwardRef<
         }
         // caret in the end + end coordinates has been changed -> we moved to a new line
         // so input may grow
+        const latestEndY =
+          latestSelection != null ? latestSelection.end.y : undefined;
         if (
           e.selection.end.position === e.selection.start.position &&
-          latestSelection?.end.y !== e.selection.end.y
+          latestEndY !== e.selection.end.y
         ) {
           return scrollFromCurrentPosition();
         }
@@ -389,9 +405,12 @@ const KeyboardAwareScrollView = forwardRef<
     useAnimatedReaction(
       () => input.value,
       (current, previous) => {
+        if (current == null || previous == null) {
+          return;
+        }
         if (
-          current?.target === previous?.target &&
-          current?.layout.height !== previous?.layout.height
+          current.target === previous.target &&
+          current.layout.height !== previous.layout.height
         ) {
           // input has changed layout - let's check if we need to scroll
           // may happen when you paste text, then onSelectionChange will be
diff --git a/src/components/KeyboardToolbar/Arrow.tsx b/src/components/KeyboardToolbar/Arrow.tsx
index 5a1290d99aa4c003e2fa386d7b0c28441d75f9b6..4d66aaea9d6019dfb0298263d5dba6750b4b2d28 100644
--- a/src/components/KeyboardToolbar/Arrow.tsx
+++ b/src/components/KeyboardToolbar/Arrow.tsx
@@ -68,12 +68,12 @@ const styles = StyleSheet.create({
   arrowLeftLine: {
     ...arrowLine,
     transform: [{ rotate: "-45deg" }],
-    left: -0.5,
+    left: -1 / 2,
   },
   arrowRightLine: {
     ...arrowLine,
     transform: [{ rotate: "45deg" }],
-    left: -5.5,
+    left: -11 / 2,
   },
 });
 
